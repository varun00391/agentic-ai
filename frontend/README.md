# v2 frontend

React and Tailwind UI for expense capture. It talks to the backend through
same-origin `/api` (Vite proxy locally, nginx in Docker).

```bash
cd v2/frontend
npm install
npm run dev
```

The shared environment file is `v2/.env`.
