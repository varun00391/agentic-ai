 Suggested build order

1. LLM extraction + confidence on extract_expense_fields (biggest quality win).

2. request_human_review + Approve / Edit API + drawer buttons.

3. Planner: goal + retries + vision fallback; allow constrained arguments.

4. Org policy table + merchant memory from corrections.

5. Multi-item path through the agent loop.